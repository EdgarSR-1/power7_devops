# DEPRECATED

Esta carpeta fue consolidada en `./scripts/` en la raíz del repo.

**Usa siempre desde la raíz:**

```bash
./scripts/setup.sh
./scripts/build.sh
./scripts/deploy.sh
./scripts/undeploy.sh
```

No uses estos scripts de aquí. Son legacy.

Si ves referencias a `MtdrSpring/scripts/` en la documentación, actualiza a `./scripts/` (raíz).

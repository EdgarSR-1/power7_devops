# DEPRECATED

Esta carpeta fue consolidada en la raíz del repo bajo `../scripts/`.

Los scripts aquí NO son el entrypoint. Usa:

```bash
./scripts/build.sh
./scripts/setup.sh
./scripts/deploy.sh
./scripts/undeploy.sh
```

(Desde la raíz del repo, no desde esta carpeta.)

Los archivos aquí pueden ser eliminados.

apiVersion: v1
kind: Secret
metadata:
  name: frontendadmin
type: Opaque
data:
  # Reemplazar por valores base64 si se usa manifiesto directo
  username: REPLACE_WITH_BASE64_USERNAME
  password: REPLACE_WITH_BASE64_PASSWORD

# Alternativa (recomendada para desarrollo):
# kubectl create secret generic frontendadmin --from-literal=username=admin --from-literal=password=REPLACE_ME

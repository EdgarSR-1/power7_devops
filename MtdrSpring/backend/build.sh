#!/usr/bin/env bash
# LEGACY: Este script ya no es el entrypoint.
# Usa ./scripts/build.sh desde la raíz del repo.
#
# Si necesitas llamar esto directamente (por razones especiales),
# asegúrate de que .env.local existe en este directorio.

echo "AVISO: Este script es legacy."
echo "Usa: ./scripts/build.sh desde la raíz del repo."
exit 1
